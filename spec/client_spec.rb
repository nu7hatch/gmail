require 'spec_helper'

describe "Gmail client" do
  context "on initialize" do
    it "should set username, password and options" do; end
    it "should convert simple username to Gmail email" do; end
    it "should detect :xoauth options" do; end
    it "should not login on initialize" do; end
  end
end
